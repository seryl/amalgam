# VolumeNodeAffinity defines constraints that limit what nodes this volume can be accessed from.
{
  required | optional | NodeSelector | doc "required specifies hard node constraints that must be met.",
}
