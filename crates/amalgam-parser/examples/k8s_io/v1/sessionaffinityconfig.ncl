# SessionAffinityConfig represents the configurations of session affinity.
{
  clientIP | optional | ClientIPConfig | doc "clientIP contains the configurations of Client IP based session affinity.",
}
