# TypeChecking contains results of type checking the expressions in the ValidatingAdmissionPolicy
{
  expressionWarnings | optional | Array ExpressionWarning | doc "The type checking warnings for each expression.",
}
