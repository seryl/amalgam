[package]
name = "amalgam-daemon"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[dependencies]
amalgam-core = { path = "../amalgam-core" }
amalgam-parser = { path = "../amalgam-parser" }
amalgam-codegen = { path = "../amalgam-codegen" }
tokio.workspace = true
kube = { workspace = true, optional = true }
anyhow.workspace = true
tracing.workspace = true
tracing-subscriber.workspace = true

[features]
default = []
kubernetes = ["kube"]